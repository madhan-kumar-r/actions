on:
  push:
    branches:
      - Dev

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: Dev
    steps:
      - name: Use secret
        run: echo "Deploying with token ${{ secrets.DEPLOY_TOKEN }}"
